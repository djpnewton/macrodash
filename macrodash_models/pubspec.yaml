name: macrodash_models
description: Models for the Macrodash app
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.7.2

# Add regular dependencies here.
dependencies:
  equatable: ^2.0.7
  json_annotation: ^4.9.0
  json_serializable: ^6.9.4
  # path: ^1.8.0

dev_dependencies:
  build_runner: ^2.4.15
  lints: ^5.0.0
  test: ^1.24.0
